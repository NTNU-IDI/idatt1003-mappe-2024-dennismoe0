
  // Se på krav til javadoc (paragraf + html greier)



  Husk å gjøre at alle priser får NOK etter seg.


  // HUSK Å FIKSE SÅ 1000g eller 1000ml blir 1kg eller 1l
  // - Change unitconverter
  // kun for totalquantity, individuelle kan være i gram/ml typ

  // Initialize everything

  // Datasets

  // 30-40 Ingredients
  // Use different measuring units to test combinations etc.
  // Also remember stuff like eggs (which are not measured in grams etc.)
  // Set the quantity of a pack of 12 eggs to 12 (base weight) so that when a user
  // uses 1 egg, it's 1/12 of the base weight.

  // Add 30 to the fridge.
  // 10 with expiration date in 1 week.
  // 2 of these have more than 1 instance in the fridge.

  // 10 with expiration date in 2 weeks.

  // 5 with expiration date in 3 weeks.

  // 5 expired.

  //
  // 10 Recipes
  // Use different ingredients.
  // Some cant be fulfilled.
  // Some have expired items in the fridge.

  // 3 CookBooks?
  // 1 with 5 recipes.
  // 1 with 3 recipes.